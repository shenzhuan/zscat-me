package

    ${package}.${

        module }.service;
    ${package}.${module}.entity.${Module}${className};

/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */

        public interface I${Module}${className}Service extends IService<${Module}${className}>{

        }
