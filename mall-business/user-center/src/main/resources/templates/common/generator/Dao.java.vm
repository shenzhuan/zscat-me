package

    ${package}.${

        module }.mapper;
    ${package}.${module}.entity.${Module}${className};


/**
 * ${comments}
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */

        public interface ${Module}${className}Mapper extends BaseMapper<${Module}${className}>{

    ${Module}${className} get(Long id);

        List<${Module}${className}>list(${Module}${className} ${classname});

        int count(${Module}${className} ${classname});

        int save(${Module}${className} ${classname});

        int update(${Module}${className} ${classname});

        int remove(Long id);

        int batchRemove(${pk.attrType}[]${pk.attrname}s);
        }
