package

    ${package}.${

        module }.service.impl;
    ${package}.${module}.entity.${Module}${className};
    ${package}.${module}.mapper.${Module}${className}Mapper;
    ${package}.${module}.service.{Module}${className}Service;

/**
 * <p>
 * 服务实现类
 * </p>
 *
 * @author zscat
 * @since 2019-04-14
 */
        @Service
        public class ${Module}${className}ServiceImpl extends ServiceImpl<${Module}${className}Mapper, ${Module}${className}>implements I${Module}${className}Service{
        }
