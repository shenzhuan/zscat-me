server:
  port: 12003

mall:
  nacos:
    server-addr: localhost:8848

spring:
  application:
    name: txlcn-demo-spring-service-c
  cloud:
    nacos:
      discovery:
        server-addr: ${mall.nacos.server-addr}