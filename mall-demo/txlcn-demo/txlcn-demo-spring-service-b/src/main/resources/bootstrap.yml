server:
  port: 12002

mall:
  nacos:
    server-addr: localhost:8848

spring:
  application:
    name: txlcn-demo-spring-service-b
  cloud:
    nacos:
      discovery:
        server-addr: ${mall.nacos.server-addr}