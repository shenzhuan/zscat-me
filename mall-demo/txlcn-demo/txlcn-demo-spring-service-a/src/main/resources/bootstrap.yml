server:
  port: 12011

mall:
  nacos:
    server-addr: localhost:8848

spring:
  application:
    name: txlcn-demo-spring-service-a
  cloud:
    nacos:
      discovery:
        server-addr: ${mall.nacos.server-addr}